__pycache__/
*.pyc
stats.json

# A local one-off that reads a set.mm on disk and writes stats.json. Kept out
# of the repository on purpose; the measurements it produced are in
# SELECTION.md.
parley/mmstats.py

# The library is fetched, never committed. A stray copy is 51 MB and
# arrives by `cp` following a symlink into a directory of elaborated proofs.
set.mm

# The verifier belongs to metamath and is fetched the same way, so that what
# checks these proofs is not a copy this project maintains.
mmverify.py
