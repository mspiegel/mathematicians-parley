theorem range-nat
  let n ∈ ℤ                                                           (H1)
  let k ∈ {1, …, n}                                                   (H2)
  then k ∈ ℕ

1.  k ∈ ℕ
    thm:stdlib/sums/range-nat, from H1, H2

theorem range-member
  let n ∈ ℤ                                                           (H1)
  let k ∈ ℝ                                                           (H2)
  then k ∈ {1, …, n} ↔ k ∈ ℕ and k ≤ n

1.  k ∈ {1, …, n} ↔ k ∈ ℕ and k ≤ n
    thm:stdlib/sums/range-member, from H1

theorem range0-member
  let n ∈ ℕ₀                                                          (H1)
  let k ∈ ℝ                                                           (H2)
  then k ∈ {0, …, n} ↔ k ∈ ℕ₀ and k ≤ n

1.  k ∈ {0, …, n} ↔ k ∈ ℕ₀ and k ≤ n
    thm:stdlib/sums/range0-member, from H1

theorem range-single
  let m ∈ ℤ                                                           (H1)
  then {m, …, m} = {m}

1.  {m, …, m} = {m}
    thm:stdlib/sums/range-single, from H1

theorem range-empty
  then {1, …, 0} = ∅

1.  {1, …, 0} = ∅
    thm:stdlib/sums/range-empty

theorem range-split
  let m ∈ ℤ                                                           (H1)
  let n ∈ ℤ                                                           (H2)
  let k ∈ {m, …, n}                                                   (H3)
  then {m, …, n} = {m, …, k} ∪ {k + 1, …, n}

1.  {m, …, n} = {m, …, k} ∪ {k + 1, …, n}
    thm:stdlib/sums/range-split, from H3
